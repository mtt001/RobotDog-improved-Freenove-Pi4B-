version: '3.8'
services:
  mediamtx:
    image: bluenviron/mediamtx:latest
    container_name: robotdog-mediamtx
    restart: unless-stopped
    network_mode: host
    volumes:
      - ./mediamtx.yml:/mediamtx.yml:ro
    command: ["/mediamtx", "/mediamtx.yml"]
